# به‌روزرسانی چت لحظه‌ای

- پیام متنی با Socket.IO و ACK ارسال می‌شود و در رابط کاربری به‌صورت optimistic نمایش داده می‌شود.
- ارسال چندباره با شناسه پیام یکتا در سرور idempotent شده است.
- وضعیت آنلاین/آفلاین بر اساس اتصال‌های فعال هر دستگاه و در سطح همان اتاق مدیریت می‌شود.
- تصویر، ویدیو، صدا و فایل به‌صورت مشترک در سرور ذخیره و در چت نمایش داده می‌شوند.
- پاسخ (Reply) به پیام اضافه شده است.
- ساعت پیام‌ها از زمان محلی دستگاه به تاریخ/ساعت شمسی تبدیل می‌شود.
- اعلان محلی برای پیام‌های ورودی اضافه شده است.

## اعتبارسنجی
در محیط ساخت فعلی Flutter/Dart SDK نصب نیست؛ بنابراین `flutter analyze` و APK در این محیط اجرا نشده‌اند. کد سرور با `node --check` بررسی می‌شود.

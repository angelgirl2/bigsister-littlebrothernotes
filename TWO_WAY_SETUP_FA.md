# Big Sister Notes — دفتر مشترک بدون کد ❤️🫂

## چیزی که کاربر می‌بیند

هیچ «کد دفتر»، «PIN جفت‌سازی»، QR یا صفحه‌ی وارد کردن کد وجود ندارد.

هر نفر فقط یک بار از مسیر زیر وارد می‌شود:

```text
تنظیمات
→ دفتر مشترک
→ من / آبجی
→ رمز ورود
→ ورود و اتصال خودکار
```

پس از ورود، Session امن روی همان دستگاه ذخیره می‌شود. در اجرای بعدی برنامه خودش به Railway وصل می‌شود و دفتر را همگام می‌کند؛ کاربر دوباره چیزی وارد نمی‌کند.

## دو حساب روی Railway

Railway دو حساب دارد:

- `LITTLE_BROTHER_PASSWORD` برای «من»
- `BIG_SISTER_PASSWORD` برای «آبجی»

این رمزها فقط روی Railway قرار می‌گیرند و داخل APK نوشته نمی‌شوند. هر دو حساب به یک فضای مشترک واحد متصل هستند.

## راه‌اندازی Railway

```bash
cd server
cp .env.example .env
```

این موارد را حتماً تغییر بده:

```env
POSTGRES_PASSWORD=یک-رمز-قوی
JWT_SECRET=یک-راز-طولانی-و-تصادفی
SHARED_ROOM_KEY=یک-کلید-داخلی-تصادفی
LITTLE_BROTHER_PASSWORD=رمز-حساب-من
BIG_SISTER_PASSWORD=رمز-حساب-آبجی
```

سپس:

```bash
docker compose -f docker-compose.prod.yml up -d --build
```

## نتیجه

هر چیزی که یکی ایجاد یا ویرایش کند—یادداشت، چک‌لیست، نامه، خاطره، تاریخ، عکس، صدا و محتوای چت—در فضای مشترک ذخیره می‌شود و روی دستگاه دیگر هم نمایش داده می‌شود. Socket.IO برای اعلان تغییرات زنده و Sync برای همگام‌سازی دوباره در شروع/بازگشت اینترنت استفاده می‌شود.
